# webgis-geodetic

## References

## Notes

+ Build Docker with `nginx`

## Usage

```bash
# development
docker compose up -d
```

## TODO

- [ ] Method add `url` and convert to `menu-item` in webgis
